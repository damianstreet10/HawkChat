# Hardware Support documents

Add PDFs (or TXT/MD) for hardware support questions, for example:

- Camera rig setup guides
- Calibration procedures
- Troubleshooting playbooks

Then restart the app (or delete `data/hawkchat.db` and reload) so HawkChat indexes them into the **Hardware Support** notebook.
