# Moved

Documents for the **Useful Resources** notebook now live in:

`seed/useful-resources/`

Hardware support PDFs go in:

`seed/hardware-support/`

See `seed/manifest.json` and `docs/LAN_DEMO.md`.
